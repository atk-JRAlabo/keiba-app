import React from "react";
import ReactDOM from "react-dom/client";
import KeibaSimulator from "./KeibaSimulator.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <KeibaSimulator />
  </React.StrictMode>
);
